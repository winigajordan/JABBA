{% extends 'base.pages.html.twig' %}
{% block title %} Gestion boutique | commandes {% endblock %}
{% block body %}
    <div class="container">
        <div class="card p-3">
            <div class="card-header">
                Commandes de ma boutiques
            </div>
            <div class="card-body">
                <table class="table table-striped table-striped">
                    <thead>
                    <tr>
                        <th scope="col">Reference</th>
                        <th scope="col">Produit</th>
                        <th scope="col">Quantité</th>
                        <th scope="col">Montant vente</th>
                        <th></th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in ventes %}
                    <tr>
                        <th scope="row">{{ item.slug }}</th>
                        <td>{{ item.produit.libelle }}</td>
                        <td>{{ item.quantite }}</td>
                        <td>{{ item.prix }}</td>
                        <td>
                            <a class="btn btn-xs btn-primary" href="#">Valider</a>
                            <a class="btn btn-xs btn-warning" href="#">Annuler</a>
                        </td>
                    </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
